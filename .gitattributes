# Normalize line endings to LF in the repo (consistent cross-platform diffs).
* text=auto eol=lf

# Treat common binaries as binary (never normalize / never diff as text).
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.svg text
*.woff binary
*.woff2 binary
