This project is built from SPEC.md — its §2 decisions are final, do not revisit.
TypeScript strict, no `any`. Log every assumption in DECISIONS.md.
Work in vertical slices per SPEC §9, commit per slice.