/** App-level configuration (per-deployment, not per-brand). */
export const APP = {
  /**
   * Deep link to the Telegram bot that hosts this Mini App, used by the
   * outside-Telegram fallback (QR + button). Placeholder until the bot ships
   * (slice 8) — set this to the real `https://t.me/<bot>` then.
   */
  botUrl: 'https://t.me/your_storefront_bot',
};
